# Brand assets

| file | source | used in |
|---|---|---|
| `tgpod-horizontal.svg` | supplied `logo 2.svg` (864×135, white) | Nav |
| `tgpod-stacked.svg` | supplied `logo 1.svg` (424×283, white) | Footer |
| `truegamers.svg` | recovered from approved TGPodEN.html | "Supported by" row (About) |

Missing / wanted:

- **Dark version of the TG Pod logo** — needed if the logo ever has to sit on the light (paper) sections. Currently it is only used on dark surfaces.
- **Square icon version of the logo** for a richer favicon / app icon set. The current `public/favicon.svg` is the approved TG monogram from the original landing.
- **Partner logos** for the "Supported by" row — only True Gamers is available; add more SVGs here and extend `about.supported` in `lib/content.ts`.
